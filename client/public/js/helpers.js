export function renderMessage(text){var message=document.createElement("div");message.innerHTML=text;message.className="pop-up-message";document.body.append(message);requestAnimationFrame(()=>message.classList.add("pop-up-message--fade-in"));setTimeout(()=>{message.ontransitionend=()=>message.remove();message.classList.remove("pop-up-message--fade-in")},2e3)}export function isInputValid(inputValue,regexString){var regex=new RegExp(regexString);if(inputValue.match(regex)){return true}return false}export function loadImage(imageUrl){var img=new Image;img.src=imageUrl;return img}export function getRandomInt(min,max){var minCeil=Math.ceil(min);var maxFloor=Math.floor(max);return Math.floor(Math.random()*(maxFloor-minCeil+1))+minCeil}